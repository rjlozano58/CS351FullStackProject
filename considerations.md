adding a bloom filter for checking if existing information already exists

firebase already checks for exsiting emails, so that application of bloom filter is taken. however, we can still check for existing posts with same title, or even add a moderation list and check for words in there and validate input based on a no no list.